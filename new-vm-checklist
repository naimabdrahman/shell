## epel
yum -y install epel-release

## firewall 
service firewalld stop
chockonfig firrewalld off

## selinux
setenforce 0
selinux off
chronyd on

estblish ssh keys 
  >> cd ~/.ssh ; ssh-keygen -f id_rsa -t rsa -N ""
  >> yum -y install sshpass
  >> sshpass -p 'password' ssh-copy-id -o StrictHostKeyChecking=no root@localhost


useradd
visudo
vim 
git
hostname - for cluster mode
/etc/hosts - for cluster mode 
